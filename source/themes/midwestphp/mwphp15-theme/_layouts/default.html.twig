<!DOCTYPE html>
<html>
    <head>
        <title>
            {% if page.title == 'Home' %}
                {{ site.title }} {% if site.description %}| {{ site.description }}{% endif %}
            {% else %}
                {% block title %}{{ page.title }}{% endblock %} | {{ site.title }} &mdash; {{ site.subtitle }}
            {% endif %}
        </title>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="/{{ theme_path('assets/css/style.css') }}" rel="stylesheet" type="text/css" />
        <!-- HTML5 shim, for IE6-8 support of HTML5 elements -->
        <!--[if lt IE 9]>
            <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
        <![endif]-->
        <link rel="alternate" type="application/atom+xml" href="{{ site.url }}/atom.xml" title="{{ site.title }} activity feed" />
        <style>
        /** quick fix because bootstrap <pre> has a background-color. */
        pre code { background-color: inherit; }
        </style>
    </head>

    <body class="no-js">
        <div class="wrapper">
            <header class="masthead" role="banner">
                <div class="navbar">
                    <div class="inner-wrapper">
                        <div class="logo">
                            <a class="brand" href="{{ site.url }}/">{{ site.title }}</a>
                        </div>

                        <nav class="primary-nav" role="navigation">
                            <div class="menu-toggle"></div>
                            <ul class="menu">
                                <li><a href="{{ site.url }}/speakers">Speakers</a></li>
                                <li><a href="{{ site.url }}/sessions">Sessions</a></li>
                                <li><a href="{{ site.url }}/schedule">Schedule</a></li>
                                <li><a href="{{ site.url }}/sponsors">Sponsors</a></li>
                                <li><a href="{{ site.url }}/venue">Venue</a></li>
                                <li><a href="{{ site.url }}/about">About</a></li>
                                <li><a href="{{ site.url }}/contact">Contact</a></li>
                                <li class="register"><a href="{{ site.url }}/register">Register</a></li>
                            </ul>
                        </nav><!-- .primary-nav -->
                    </div>
                </div>
            </header><!-- .masthead -->

            <main class="main-content inner-wrapper{% if page.hasSidebar %} with-sidebar{% endif %}">
                <article class="page-content">
                    <h1>{{ page.title }}</h1>
                    {% block content_wrapper %}{% block content %}{% endblock %}{% endblock %}
                </article>

                {% if page.hasSidebar %}
                <aside class="sidebar">
                    <h1>Sidebar</h1>
                    <p>Not sure what we'll put here just yet - possibly sponsor info and/or maybe our MailChimp signup?
                        If we're placing logos, the blue background will likely be a bit much, but I wanted to get some
                        kind of dynamic sidebar mechanism in place. Also, this text is currently in the default layout file
                        and will need to be moved into its own content file.</p>
                </aside>
                {% endif %}
            </main>

            {% include 'footer' %}

        </div><!-- .wrapper -->
        <div class="mobile-nav-menu"></div><!-- placeholder for mobile nav -->

        <script src="//ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
        <script>window.jQuery || document.write('<script src="{{ site.url }}/components/jquery/jquery.min.js"><\/script>')</script>
        <script src="/{{ theme_path('assets/js/main.js') }}"></script>

        {% block scripts %}
        {% endblock %}

        {% if site.google_analytics_tracking_id %}
        <script>
          (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
          (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
          m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
          })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

          ga('create', '{{ site.google_analytics_tracking_id }}', 'auto');
          ga('send', 'pageview');

        </script>
        {% endif %}
    </body>
</html>
